# レビューゲートのローカル状態(セッション別の編集記録・レビュー済みコミット台帳。ADR-0045)
/.claude/state/

# Rust実装開始後のビルド成果物
/target/

# 生成JSの実行検証(E2Eテスト)で使うNode関連
node_modules/

# 旧方式のマーカー(ADR-0045で廃止)。既存の作業ツリーに残った分を無視し続ける
.claude/.doc-review-marker
.claude/.impl-review-marker
