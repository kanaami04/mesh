# CLAUDE.md — Musubi プロジェクト作業ルール

> `AGENTS.md`(AIコーディングツール向けの業界標準の置き場所)はこのファイルへのシンボリックリンク。内容はここで一元管理する。

## プロジェクト概要

プログラミング言語 **Musubi**(結び)の開発。FE+BE両対応・人間に読みやすい・AIに書きやすい言語を、**Rust製コンパイラでJavaScriptにトランスパイル**する方式で、完全にゼロから作る。

## 協働スタイル

- ユーザーはコードを書かず、Claudeが実装する。**すべての実装・決定には日本語の解説を添え、ユーザーが仕組みを学べるようにする**(何をしたかだけでなく「なぜ」「他の選択肢は何だったか」を説明する)。
- 大きな設計決定はユーザーと相談して決める。決めたらADRに残す。

## 文章ルール(常時遵守)

docs/の全文書とClaudeの解説は **docs/style-guide.md** に従う。核心は5つ:

1. 結論を先頭に(文書・セクション・段落すべて)。
2. 一文一義。読点3つを超える文は分割する。
3. 専門用語は初出で短く説明し、以後は同じ語で統一する。
4. 抽象語(「適切に」「など」)で逃げず、例・数値・条件で具体化する。
5. 将来のMusubiのエラーメッセージもこのガイドの原則4(短く・責めない・平易・修正候補つき)で設計する。

## 正文書ルール(ドキュメントdrift対策)

「仕様書が古いまま放置され、後続の決定と矛盾する」driftを防ぐため、文書の優先順位を固定する:

1. **ADR(docs/adr/)が意思決定の唯一の正**。矛盾があれば番号の新しいADRが勝つ。
2. **docs/spec/ は仕様の正**だが、ADRと矛盾したらADRが勝ち、spec側を直す義務が生じる。
3. docs/vision.md は方向性のみ。個別仕様の根拠には使わない。
4. **決定を覆すときは必ず新ADRを書き、旧ADRのStatusを superseded にし、docs/adr/ASSUMPTIONS.md の関連前提を更新する**。この手順を飛ばして仕様変更をしない。

## スペック駆動の機能開発(実装フェーズ開始後)

機能単位の作業は「**requirements → design → tasks → 実装 → 検証**」の順で進める(spec-kit / Kiro流)。仕様が正、テストが執行者。

- 受け入れ基準は**EARS風**の文(「〜のとき、システムは〜すること」)で書き、そのままテストケースに落とす。
- 仕様に書いた正例・負例は tests/ のconformanceテストと1:1対応させる。仕様に書いたのにテストが無い状態を作らない。
- 着手前にタスク分解を提示し、大きな機能はフェーズのroadmap完了条件と紐づける。

## リポジトリ内スキル(.claude/skills/ — Agent Skills標準)

- `adr` … 決定が確定したら自動発動(または `/adr` で明示呼び出し)。採番・索引・前提台帳・spec修正まで一括で行う。
- `doc-review` … md文書を実質的に更新した作業の締めに**自発的に**実行する変更単位レビュー(設計矛盾・相互作用の穴・文言品質・サンプルコードのdrift)。ユーザーの指示を待たない。**Stopフック(.claude/hooks/doc-review-check.sh)が未実行を検知して強制する**ため、忘れても機械的に発動する。
- `drift-check` … ADR・spec・CLAUDE.md間の矛盾を全量検査してレポートする。フェーズ完了時に必ず実行する。
- `spec-write` … 仕様章の執筆・改訂手順。ADRをEARS形式+正例・負例に展開し、負例へのconformanceテストID予約まで行う。
- `spec-review` … 仕様章の新規執筆・大改訂の品質ゲート。独立サブエージェントの敵対的レビュー(3ペルソナ・LGTM禁止)→修正→同一エージェントによる解消検証を、新規要修正ゼロまで反復する。spec-writeの締めで必ず実行。
- `tdd` … 実装フェーズのRed→Green→Refactorワークフロー。このプロジェクトで洗練させ、安定したらグローバル(~/.claude/skills/)へ昇格させる予定。

## 作業の終え方(マージ可能ルール)

作業単位の締めは**マージ可能な状態**まで持っていく: doc-review済み(実装フェーズではテスト・`cargo fmt`・`cargo clippy` も緑)→ 作業ブランチに意味単位でコミット → push → **PRを作成**(コンフリクトなし・チェック緑)。未コミットの変更を残したまま作業を終えない。

- **基本はPR提出**: mainへ直接pushせず、作業ブランチからPRを作って引き渡す。ユーザーが「PRなしで」「直接mainへ」と言ったときだけ直接pushする。
- **pushの有無は選べる**: 「pushは保留」「コミットまで」と言われたらローカルコミット止めにし、報告に「push待ち」と明記する。指定がなければpush+PR作成まで行う。

## コンテキストファイルの原則

このCLAUDE.md(=AGENTS.md)は**最小・正確・人間レビュー済み**を保つ(2026年の研究で、肥大化したAI生成コンテキストファイルはエージェントの成功率を下げコストを上げると報告されている)。追記するときは「毎セッション読む価値があるか」を基準にし、詳細はdocs/へ逃がす。大きな変更はユーザーの確認を得る。

## 実装ルール(実装フェーズ開始後)

- **実装は `tdd` スキルのRed→Green→Refactorサイクルで進める**。テストが先、失敗確認が先。スナップショットテストはサイクル完了後の回帰の網として使う(スキル内の併用ルール参照)。
- Rust edition 2024、`cargo fmt` + `cargo clippy` をクリーンに保つ。
- crate構成は最初は最小(`crates/musubi` 1つ)から始め、痛みが出てから分割する。
- パーサは**手書き再帰下降**(エラーメッセージ品質を最優先するため。パーサジェネレータは使わない)。
- テストは docs/testing-strategy.md に従う。**スナップショットテスト(insta)が主力**。新機能はスナップショット+負例(エラーになるべきコード)をセットで追加。
- 生成JSの実行検証はNode.jsで行う(E2Eテスト)。

## 用語

- 言語名: Musubi / CLI: `musubi` / 拡張子: `.msb`
- 「ターゲット」= コンパイル出力先(現状JSのみ。将来wasm追加の可能性をIR設計で担保)
