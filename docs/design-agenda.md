# 設計アジェンダ — コンセプトメモとの突き合わせ(2026-07-17)

> [concept-memo.md](concept-memo.md)(別セッションの記録)と、このリポジトリの決定事項
> ([features.md](features.md) / [requirements.md](requirements.md))を突き合わせた結果。
> 3分類: **A. 一致**(追認) / **B. 衝突**(要決着) / **C. 新規アイデア**(未議論)

## A. 一致している(すでに両者で同じ結論)

| 項目 | 状態 |
|---|---|
| コアコンセプト(AIが書き・人が読む/一つの書き方/ローカル推論/修正を示すエラー/学習データ近さ) | requirements の P1〜P5 と同内容。メモの「型注釈=圧縮されたコンテキスト」という言語化は P3 の補強として採り入れたい |
| 波括弧デリミタ(インデント構文不採用) | v0 実装済み。メモの根拠「AIのdiff/部分編集に空白構文は脆い」は新しい論拠として記録価値あり |
| match式採用・switch不採用・網羅性チェック | 2026-07-17 に同結論で決定済み |
| enum不採用・union型採用 | 決定済み(ただしメモはさらに先へ → B-1, C-1) |
| 例外なし・エラーは値 | 決定済み(ただし表現形式が違う → B-2) |
| JSターゲット・フロント/バック型共有 | P6 と一致。APIコントラクト構想は C-7 へ |
| Go風並行処理・型付きchannel・関数の色問題の解消 | 決定済み(spawn)。「全関数async化」で実装済みのため、メモの懸念のうちCPS変換問題は既に回避されている |
| フォーマッタ一択・設定なし | mesh fmt として決定済み |
| 関数シグネチャの型注釈必須 | v0 実装済み(公開関数だけでなく全関数で必須。メモより厳しい側で一致) |
| コンパイラの構造化出力(JSON) | mesh check --json として決定済み |
| 並行≠並列の割り切り明記 | features.md に記載済み |

## B. 衝突している(要決着)

### B-1. 空値の設計 — ✅ 決着(2026-07-17): `T | none` を採用(union路線)

- **このセッションの決定**: `nil`→`null` 改名。空値は null 1つ。null は error/chan/配列/関数型に代入可
- **メモの決定**: null 無し。不在は `T | none` の union で表現し、match / if narrowing で安全に取り出す
- **論点**: null は「どこにでも入り得る値」なので、チェッカーは「null かもしれないのに使った」を
  検出できない(10億ドルの過ち)。`T | none` は不在を**型に**するので、取り出しに narrowing が
  強制され、未処理がコンパイルエラーになる。union+match+網羅性を採用済みの現在、
  `T | none` の方が一貫する
- **影響範囲**: 大。checker に narrowing(型の絞り込み)実装が必要。features.md の null 行の書き換え

### B-2. エラーの表現 — ✅ 決着(2026-07-17): `T | error` を採用(union路線)。多値戻りは廃止。`!`/`or` は union 上で維持

- **このセッションの決定**: Go流 `(T, error)` 多値戻り + `!` 伝播 + `or`(v0実装済み・E1/E2採用済み)
- **メモの方向**: Result的union `{ kind: "ok", value: T } | { kind: "err", error: E }` を返す
- **論点**: B-1 と本質は同じで、**「不在・失敗を『特別な値』で表すか『union型』で表すか」**という
  言語の背骨の選択。union路線なら エラー/不在/channelのclose(C-5)まで全部
  「union + match + 網羅性」の一つの原理で説明できる(概念の数が最小になる)。
  Go路線は学習データ・v0実装・既決定(E1/E2)との連続性が強み
- **`!`/`or` への影響**: union路線にしても `!`(Rustの `?` 相当)や `or` は定義し直せる。
  ただしメモは「見えない早期リターンの復活」として `?` 系そのものに慎重論を残している
  (E1採用の再確認が必要)
- **影響範囲**: 特大。エラー処理は言語の顔。決めたら仕様書に固定して以後動かさない

### B-3. select — ✅ 決着(2026-07-18): matchの見た目を踏襲した独立構文で実装

`select { v := <-ch1 => body  v := <-ch2 => body  _ => body }`。実装してみると
match の型パターンとは本質的に別物(「型が何か」ではなく「どの受信操作が先に終わったか」)と
判明したため、match を乗っ取らず独立構文にした(見た目のみ踏襲)。複数同時準備なら
擬似ランダム選択(Goと同じくcase飢餓を防ぐ)。送信ケースは無し(Meshのchannelは既定
無制限バッファのため送信不可の待ち合わせはほぼ無意味)。C-5と同時実装。テスト206件

### B-4. 宣言構文の流儀 — ✅ 決着(2026-07-17): `:=` 不変デフォルト + `mut`

決定内容: `x := 10` は不変束縛 / 可変は `mut count := 0` のみ / 引数は常に不変 /
C風forのヘッダ変数だけ暗黙可変(唯一の構造的例外) / v1の不変性は束縛レベル
(JSのconst相当。中身の深い不変性は将来検討)。キーワードは `var` 案も検討したが
`mut`(Rustの let mut 相当)に決定。`let` 単独案は「JSのlet(可変)と同字面で逆の意味」
になるため不採用。

- **このセッションの決定**: `:=` + シャドーイング禁止(実装済み)。const は不採用
- **メモの提案**: デフォルト不変 `let` + 可変明示 `mut`(レビューで mut だけ注視すればよい)
- **論点**: 「デフォルト不変」はレビュー・AI出力の安定に効く実績ある設計(Rust)。
  ただし宣言キーワードが増え、`:=` の簡潔さと衝突する。中間案もある:
  `:=` を不変束縛とし、可変にしたい変数だけ `mut x := 0` と書く(1キーワード追加で済む)
- **付随**: メモのコード例は TS 風味(`fn f(): T` のコロン戻り値、named args)で、
  現行のGo風味と表層構文の流儀が揺れている。B-1/B-2 の決着後に構文仕様書で統一する

### B-5. 型宣言の構文(追記・同日決着): `struct` / `type` の分離

union路線決定後、「`type Status = ...` と `type User { ... }` の2形態が紛らわしい」との
指摘(kanayama)から T1 を改訂。**データ定義は `struct User { ... }`、名前付け・unionは
`type X = ...`** に分離。意味論は構造的型付けで統一(キーワードの違いは読み手のための
表層区別であり、Goのような alias/新型の意味差は無い)。`type =` の右辺に裸の `{...}` は
エラーにして書き方を1通りに保つ。`{...}` 型式は union 内では有効(判別可能union C-1 への道)。

## C. 新規アイデア(このセッションでは未議論)

| # | アイデア | 一言評価 |
|---|---|---|
| C-1 | ~~**判別可能ユニオン**(タグ付きオブジェクトのunion)+ narrowing~~ | ✅ 決着・実装済み(2026-07-19)。
      `type X = { kind: "ok", ... } | { kind: "notFound" }` — union内だけで無名 `{...}` 型式が書ける
      (B-5の布石どおり)。構築は union自身の名前をstructリテラル名に流用(`X{kind: "ok", ...}`、
      フィールド集合で該当メンバーを特定)。narrowingは既存のmatch機構を拡張した部分構造パターン
      `{ kind: "ok" }` で行い、絞り込み後は普通のフィールドアクセス(新しい束縛構文は増やさない、
      P1)。この実装のために struct の同一性判定を名前ベース→全面的な構造的比較に変更した
      (design-agendaの「構造的型付け」項目を前倒しで実装)。自己参照(木構造・AST等)も同日中に
      追加実装 — structフィールド越しの参照はknot-tyingで解決。structを挟まない裸のunion同士の
      相互再帰(`type A = B | none` かつ `type B = A | error`)だけは、flatten時に相手の
      placeholderがまだ空で型情報が消える不具合を避けるため、意図的に`type alias cycle`のまま |
| C-2 | **matchガード**(`case x if x > 0`相当) | 小さい追加。match採用済みなので自然な拡張 |
| C-3 | **named args**(`createUser(name: "x", age: 3)`) | AIの引数順ミス対策として有効。呼び方が2通りになるならP1と緊張。「引数2個以上は必須」等の一律ルールなら整合 |
| C-4 | **let/mut(デフォルト不変)** | B-4 に統合 |
| C-5 | ~~**channelのclose設計**~~ | ✅ 決着・実装済み(2026-07-18)。`close(ch)` + `<-ch`は常に`T \| closed`。
      二重close・close済みへの送信はpanic。`closed`は`!`/`or`の対象に含めない(B-3と同時実装) |
| C-6 | ~~**標準ライブラリの層分け**~~(core共通 / 環境別モジュール、importで環境をコンパイル時判別) | ✅ コア機構+`mesh/http` v1は決着・実装済み(詳細はI節参照)。
      **2026-07-19進捗**: 土台のモジュールシステムを実装(パッケージ=ディレクトリ・export可視性・
      `pkg.symbol`修飾。kanayamaとGo風を選択)。環境判別は「importしたモジュールから自動推定」方式に
      決定済み(`mesh/http`と`mesh/dom`の同居はエラー)。
      **2026-07-21実装**: `mesh/http` v1(サーバー専用・生ハンドラ+`http.listen`のみ、
      ルーターは無し)。DOM側(Q3=フロントエンドの形)は引き続き未決着・スコープ外 |
| C-7 | **APIコントラクトの言語ネイティブ化**(tRPC/Zod相当を言語で) | P6の最終形。モジュールシステム設計とセットで中期課題 |
| C-8 | **Promiseブリッジ/FFIの仕様化** | 未決Q2(npm相互運用)と同件。「境界でPromise→channel/多値、例外→エラー値、undefined→不在」を言語仕様として明文化する話 |
| C-9 | **UIイベントをchannelで扱う** | C-6/Q3とセットで検討。並行モデルとUIの統一感が出る面白い構想 |

## D. 推奨討議順

1. ~~B-1 + B-2(空値とエラーの統一モデル)~~ — ✅ **決着: union路線**(2026-07-17)。
   実用シナリオ比較(連鎖処理はほぼ同型 / 不在と失敗の区別 / うっかりミス表)の結果、
   「未処理・仕様変更漏れをコンパイラが捕まえる」を決め手に採用
2. **B-4(let/mut vs :=)+ 表層構文の流儀** — 背骨が決まれば見た目を統一できる
3. **C-3(named args)・C-2(matchガード)** — 小粒の採否
4. **E-1 → B-3 + C-5(channel露出の是非 → select/close などchannel仕様)** — 並行処理の完成。
   E-1(構造化並行に寄せるか)を先に決めてから select/close の詳細に入る
5. **C-6〜C-9(stdlib層分け・FFI・APIコントラクト)** — 未決Q2/Q3と統合して設計
6. メモの未決6「構文仕様の文書化」 — 上記が済んだら言語仕様書(spec)へ移行

## E. 類似言語調査からの論点(2026-07-18追記)

MoonBit調査([related-languages.md](related-languages.md))から出た討議事項。

### E-1. channelを露出すべきか — ✅ 決着(2026-07-18): 2段スコープ設計を採用(kanayama発案)

決定内容: channel は維持しつつ、**すべてのタスクに所有者を持たせる**。
`spawn` = 囲む関数が所有(関数を抜けるとき暗黙に待つ=リーク構文的に不可能)/
`detach` = プログラムが所有(呼び出し元は待たず、プログラム終了までに完了)。
発端は kanayama の「トランザクションの defer rollback のように、後片付けを自動保証
できないか」という提案。「waitの中でしかspawnできない」制約案(B案)より強い保証を、
既存コードを一切壊さず(checker無変更・codegenのみ)実現した。
Kotlin(GlobalScope非推奨)・Swift(Task.detached)・Python(TaskGroup)と同方向で、
エスケープハッチが `detach` として可視(grep可能)なのは `mut` と同じ思想。

- **現状のMesh**: Go風 `spawn`/`wait`/channel(実装済み)。残タスクは select(B-3)と容量指定
- **MoonBitの設計**: channel無し。タスクは task group 内でのみ生成でき、group は全タスク終了後に
  のみ返る(構造化並行)。孤児タスク(goroutine泄漏)が設計上発生せず、キャンセルも組み込み
- **論点**: B-3(select)や容量指定・close設計(C-5)に進む前に、「channelは言語の顔として
  残すか、構造化並行に寄せるか」を一度決める。Meshの `wait` ブロックはすでに構造化並行に
  半歩踏み出しており、折衷(waitを唯一のspawn許可スコープにする等)もあり得る。
  P1(書き方は一つ)とP5(学習データ近さ=Goのchannel資産)が衝突する構図

### E-2. AI向けレビュー支援ツールの優先度

- MoonBitは fmt(設定なし)と**言語組み込みスナップショットテスト**を早期整備。
  「AIが書いた差分を人がレビューしやすくする」効果が大きい
- Meshでは `mesh fmt` がTODO済み。スナップショットテストは未議論 → 採否を検討
- なお `mesh check --json`・全関数の型注釈必須は MoonBit と同方向で追認材料

## F. 批評ターン起点の再討議項目(2026-07-19追記)

批評ターン([critique-2026-07.md](critique-2026-07.md)。言語設計者レンズ+実務レンズの
2独立批評+内部批評の統合)から出た討議事項。**F-1〜F-4は過去の決定の再討議**であり、
「決めたら動かさない」原則の例外として、批評で新しい根拠(実測・学習データとの負転移)が
出たものに限って再開する。

### 討議が必要(設計判断)

| # | 論点 | 一言評価 |
|---|---|---|
| F-1 | ~~**関数型注釈 → 最小ジェネリクス**~~(`f: fn(int) int` / `fn first<T>(...)`)| ✅ 決着・実装済み。
      **前半(関数型注釈・単相)2026-07-19実装**。構文は関数宣言と同じ読み(unionは戻り値側に束縛・
      関数自体のunionは括弧・パラメータ名なし)。ユーザー定義の高階関数(apply/retryInt/makeAdder/
      structフィールド/chan要素)が書けることを確認。
      **後半(ジェネリクス)2026-07-19実装(kanayama承認: 呼び出し側は引数から自動推論)**。
      トップレベル`fn`限定・制約なし(Tは完全に抽象、`+`/`.field`等の型固有操作は不可、だから
      `sort`は組み込みのまま)・明示instantiation構文は無し(`<`/`>`が式中で比較演算子と衝突する
      ため不採用。代わりに「型パラメータは必ずパラメータ型に現れる」を宣言時に強制し、常に
      引数から推論可能にした)。コード生成側の変更は無し(JSは動的型付けなので単相化不要)。
      「contains等の組み込み特権をstdlib通常関数に降格」は仕組みが入っただけで未実施 —
      降格先(自動availableなstdlibソースの置き場)は別途要討議のため今回は据え置き |
| F-2 | ~~**エラー設計の和解**~~ | ✅ 決着・実装済み。**前半(コンテキスト付加形)2026-07-19実装**:
      `f() ? "line ${i}"` — 失敗時だけ文脈を評価し `error("文脈: 元メッセージ")` として伝播
      (noneも`error("文脈")`に昇格)。
      **後半(構造化エラーの伝播対象化)2026-07-19実装(kanayama承認: 明示マーカー方式)**。
      「どのメンバーが失敗か」を判定する2案(A: `error type X = ...`で宣言側が明示 / B: unionの
      先頭=成功・残り=失敗という位置ルール)を比較 → P2(暗黙より明示)に沿うAを採用。
      `error type DbError = {...} | {...}` / `error struct DbError {...}` と宣言すると、
      `?`/`or`がそのメンバーをnone/組み込みerrorと同じく伝播対象にする。文脈つき`? "ctx"`は
      構造化エラーを弾く(メッセージへ変換できないため)。実行時はstructリテラル生成時に
      Symbolマーカーを埋め込み判定(`instanceof Error`化はしない — printやフィールドアクセスの
      挙動を普通のstructのまま保つため)。既存の名前付き型を後からタグ付けする
      (`error type X = ExistingStruct`)ことは、そのタグが他の場所へ漏れるため宣言時エラーで拒否 |
| F-3 | ~~**構造的型付けの範囲の再討議**~~ | ✅ 決着・実装済み(2026-07-19、kanayama承認)。**名前的に巻き戻し**:
      名前付きstruct同士は名前で判定(Meters→Dollarsはコンパイルエラーに)。無名`{...}`メンバーが
      絡む比較だけ構造的(名前が無いため。判別可能unionの機能・互換は全て維持)。
      検討した代替案 opt-in `distinct struct` は「安全がオプトインになる逆向き設計」を理由に不採用 |
| F-4 | ~~**伝播記号 `!` → `?` への改名**~~ | ✅ 決着・実装済み(2026-07-19、kanayama承認)。後置`?`に改名
      (Rustと記号・意味が完全一致)。旧`!`は「postfix '!' was renamed — use '?'」の誘導つき
      パースエラー。前置`!`(否定)との同字面衝突も解消 |
| F-5 | ~~**`or` のerror無音吸収**~~ | ✅ 決着・実装済み(2026-07-19、kanayama承認: **Go式の明示性**)。
      素の`or 式`はnone専用。errorを含むunionには束縛形が必須 — `or e => 式`(使う)/
      `or _ => 式`(捨てる痕跡を字面に残す。Goの`v, _ :=`のunion路線翻訳)。
      「自動でログ出力」案はP2違反・出力先はアプリの都合・テスト汚染を理由に不採用 |
| F-6 | ~~**narrowing拡張**~~ | ✅ 決着・実装済み(2026-07-19)。条件式から then側/else側で成り立つ事実(パス→絞り込み型)を
      再帰的に集める方式で一括対応: (1) フィールドパス(`if n.next is none`、rootが不変変数なら任意の深さ)、
      (2) `&&`連結(左の`is`が右辺の式とthen節の両方に効く)、(3) `!`/`||`のド・モルガン(`!(a is none)`、
      `a is none || b is none { return }`後は両方絞り込み済み)。フィールドへの代入(`n.next = ...`は
      いつでも許される)は古い事実を無効化してから型検査するので健全性は保つ。`match`の対象もついでに
      同じ仕組み(安定パス)に統一し、`match n.next { ... }`も絞り込めるようにした |
| F-7 | ~~**判別可能unionのタグ必須化**~~ | ✅ 決着・実装済み(2026-07-20、kanayama承認)。
      struct member構築の解決を「書かれたフィールド集合の完全一致」から「タグフィールドの値」へ
      全面移行。union宣言の解決時(`resolveAlias`)に、無名`{...}`メンバーが2個以上あれば
      「全メンバーに存在しリテラル型で値が互いに異なる」フィールド(`discriminantTag`)を要求し、
      無ければ宣言自体を`discriminated-union-tag-required`でエラーにする(構築時ではなく
      **宣言時**に検出するので、遠隔作用が起きようがない)。構築(`structLit`)はそのタグの値
      だけを見て対応メンバーを一意に特定 — 他メンバーのフィールド集合とは一切比較しない。
      **名前付きstruct同士のunion**(`type Shape = Circle | Square`。各々が自分の名前で構築される)
      は対象外とし、従来のフィールド集合ベース解決を維持(タグの動機である「union自身の名前でしか
      構築できない無名メンバー」の曖昧さがそもそも存在しないため)。既存の`discriminated-union-ambiguous`
      は無名メンバー経路では到達不能になったが、名前付きstruct経路では引き続き発生しうるので
      コードは維持(F-13の「削除・改名しない」契約) |
| F-8 | ~~**transform/map の再々検討**~~ | ✅ 決着・実装済み(2026-07-20、kanayama承認)。
      `map`を文脈依存キーワード化: パーサが`map`トークンの直後を1つ先読みし、`<`なら従来通り
      型/リテラル(`map<K,V>`)、`(`なら組み込み関数呼び出し(`map(arr, f)`)の識別子として読み替える。
      レキサ側の予約語扱いは変更していない(型注釈等の`ident`位置では引き続き`map`は使えない —
      この二重性はこの1箇所だけの特別扱い)。`transform`は廃止し`BUILTINS`/checker/codegenを
      全面改名。旧`transform(...)`は`undefined: 'transform'`になる(意図的な破壊的変更) |
| F-9 | ~~**小さな一貫性の穴の掃除**~~ | ✅ 決着・実装済み(2026-07-20、kanayama承認: 4項目とも採用)。
      **(a) 空の型付き配列を統一**: `Todo[]{}`(空)を廃止しパースエラーに(`empty-typed-array-literal-removed`)、
      `xs: T[] = []`に一本化。非空の`Todo[]{a, b}`(明示的な要素型指定)は引き続き有効。
      **(b) 複合代入`+= -= *= /= %=`を追加**: binary式の算術検査(`checkArithOp`)をAssign文と共有し、
      safe-integer/ゼロ除算検査も適用される。mapの要素への複合代入は欠損キーの危険があるため
      明示的に禁止(`compound-assign-on-map`。`m[k] = (m[k] or fallback) op value`を使う)。
      **(c) トップレベル定数を解禁**: `x := 10` / `x: T = 10`(常に不変、`mut`不可)。`export`で
      パッケージ越しに`pkg.name`として参照可能(関数と同じ経路)。JSの`const`はhoistされないため、
      codegen側でパッケージを依存順に並べ替える処理を追加(cli.tsの発見順は逆向きだったため)。
      **(d) `get(arr, i) → T | none`を追加**: 範囲外でもpanicしない安全な読み(既存の`arr[i]`は
      panic用途のまま残す) |
| F-10 | ~~**int 53bitのsafe-integer検査**~~ | ✅ 決着・実装済み(2026-07-20)。int同士の`+ - *`の
      演算結果が`Number.isSafeInteger`を外れたらpanic(既存の範囲外アクセス・ゼロ除算と同じ
      パニック層に追加。`__iarith`)。`/`は既存の`__idiv`が切り捨てるため対象外(結果が入力より
      小さくなる方向にしか動かない) |
| F-11 | ~~**chan既定無制限バッファの説明責任**~~ | ✅ 決着・実装済み(2026-07-20、kanayama承認:
      「capacityを常に明示必須にする」)。`chan<T>()`(capacity省略)を廃止しパースエラーに
      (`chan-capacity-required`)。無制限バッファを選ぶこと自体は`chan<T>(none)`で引き続き可能 —
      「既定で無音に無制限」だった状態を「毎回明示で選ぶ」に変えることでC-5の説明責任問題を解消 |

### 実装・計測タスク(討議より実行が主)

| # | 項目 | 一言評価 |
|---|---|---|
| F-12 | **対TS/Go比較ベンチマーク** | **第1・第2ラウンド✅ 2026-07-20実施**(ハーネスは`bench/`、
      結果は docs/benchmark-2026-07.md)。第1R(Sonnet): 一発成功率 Mesh 3/4・TS 4/4・Go 4/4。
      発見された2つの穴(複数行unionの行頭`|`・`eval`予約語のカード未記載)を即日修正し、
      回帰確認で該当セルが往復2→0 — **「ベンチで穴を見つける→直す→一発成功率が上がる」の
      改善ループを1周実証**。第2R(Haiku): Mesh 2/4・TS 3/4・Go 4/4、全セル最大1往復で成功。
      対称プロンプトではTSも`eval`(strict mode)で落ち、負転移は3言語とも浅い層(記法・予約語)に
      収束。新発見のmatch空アーム非対応はカード明記済み。第3R候補: 大型タスク・複数試行のdiff一貫性 |
| F-13 | ~~**ツールチェーン契約の強化**~~(5項目の束) | ✅ 決着・実装済み(2026-07-20、kanayama承認: 前半3項目→後半2項目の順)。
      **前半**: 診断は`code`(全169箇所の`error()`/`CompileError`呼び出しに約87種のコードを付与。
      `error()`のシグネチャ変更で「付け忘れ」をTypeScriptに強制検出させた)と、機械適用可能なら
      `fix`(単一range置換。安全に自動化できないものは無理に作らない — 実際`bare-struct-shape`は
      「1行1フィールド」のときだけ付け、カンマ書きのボディだと壊れることを実測で発見して条件を
      絞った)を持つ。`mesh explain <code>`はコードごとの一般的な説明+引数無しで全コード一覧。
      **後半**: サブセットカード(`mesh card --for <file>...`。渡したソースの簡易パターン検出で
      使っている機能のセクションだけに絞る。全機能検出時はフルカードそのまま返す=「COMPLETE
      reference」の主張を壊さない)と、カード完全性の逆方向検査(`BUILTINS`/`RESERVED`の正典
      リストとカード記載を機械照合するCIテスト)。後者は**正直な限界**があり、`s[0]`のような
      「そもそも正典リストに無い未知の見落とし」は自動発見できない — 既知リストの将来の記載漏れ
      という退行防止に留まる、と設計時点でkanayamaと合意済み |
| F-14 | ~~**mesh/io + mesh/json**~~(C-6の続き)| ✅ 決着・実装済み(2026-07-20、kanayama承認: v1は
      `io.args()`/`io.readFile(path)` + `json.parse`/`json.stringify`+`json.Value`のみ)。
      **アーキテクチャ**: `mesh/io`・`mesh/json`は`.mesh`ソースを持たない唯一のパッケージ種別
      (「組み込みパッケージ」)。`src/stdlib.ts`が型シグネチャ(`json.Value`の自己参照union型含む)
      を直接TypeScriptで構築してcheckerの`registry`へ事前登録し、`checkPackage`(通常のMesh
      ソース検査パイプライン)は一切通さない — importグラフの依存順・循環検出からも除外。
      実体(JS実装)は`runtime.ts`のPRELUDEに素のJSとして埋め込み、既存の`pkg$name`マングル
      規約にそのまま乗るのでcodegen自体の変更は不要だった。`io.readFile`はNode専用`fs`に
      動的importで依存するため、プレイグラウンド(ブラウザWorker)では例外にならず`error`に
      落ちる(クラッシュしない)。`mesh run file.mesh a b`の`a b`は子プロセスへ転送し
      `io.args()`で読めるようにした(CLI変更)。
      **副産物**: 実装中に`typeToString`が自己参照struct/union(`Tree`型でも再現)で無限再帰する
      既存バグを発見・修正(`seen`集合で再帰中の型を追跡し「...」で打ち切るガードを追加)。
      **今回のスコープ外**(将来必要なら再討議): `io.writeFile`/stdin読み取り、`mesh/http`等の
      環境別モジュールとその環境自動推定(C-6)。
      **実装メモ(2026-07-20、kanayama起点の討議より。`mesh/http`着手時のために残す)**:
      サーバー系stdlibを実装するときは、requirements.md 5.5「障害分離」方針どおり、内部のJS
      グルーコード側でリクエストのディスパッチループに`try/catch`を1箇所仕込み、ハンドラの
      panicがプロセス全体を`process.exit`させないようにすること(Goの`net/http`が内部で
      `recover`するのと同じ構図)。Mesh言語自体に`panic()`/`recover()`を追加するのではない —
      この分離はstdlib実装者の責任として1回書けばよく、Meshユーザーのコードには一切現れない。
      現行の`spawn`/`detach`(Mesh構文経由)は`__panic`固定で`process.exit`するが、stdlib内部が
      ハンドラ関数を素のJS `async`呼び出しとして直接呼べばこのルートには乗らないので、今回の
      F-14実装(組み込みパッケージのアーキテクチャそのもの)はこの将来の実装を妨げていない |
| F-15 | ~~**mesh test --json**~~ | ✅ 決着・実装済み(2026-07-20、kanayama承認: 各設計判断は推奨案どおり)。
      批評ドキュメントに具体案が無く、テストの書き方自体を新規設計した。**構文**: 新キーワードは
      追加せず既存`fn`+命名規約(`fn test...() none | error`。`_test.mesh`ファイル限定)。
      合否表現に新しい概念を作らず、既存の絶対/失敗語彙(`none`/`error`)をそのまま流用したのが
      P1的に一番の勘所。**配置**: Goの`_test.go`を踏襲し`_test.mesh`規約(通常の`.mesh`とは
      分離)。**障害分離**: テスト実行中のpanicは1件の失敗として隔離し他は続行 —
      requirements.md 5.5「障害分離はstdlib/ランタイム内部の仕事、言語からは見せない」を
      F-14で書いた備忘のとおり初めて実地適用した(ランナー内部のJSがtry/catchするだけで、
      Mesh言語に`panic()`/`recover()`は追加していない)。
      **アーキテクチャ**: checkerがfn登録時に`_test.mesh`内の`test`接頭辞fnを発見・シグネチャ検証し
      (`CheckResult`に`tests`を追加。`checkPackage`/`checkModules`の返り値を拡張)、
      codegenがtestMode時に`main().catch(__panic)`の代わりに`__runTests([...])`ハーネス呼び出しを
      発行、CLIが`mesh test <file|dir>`でファイル/ディレクトリ両対応のロード(依存先パッケージは
      本体コードのみ・自身のテストは実行しない)と結果表示(`--json`/人間向け)を行う |

### G. 推奨討議順(F節)

1. ~~**F-1(関数型注釈→ジェネリクス)**~~ ✅ 2026-07-19実装(前半・後半とも完了)
2. ~~**F-2(エラー設計)**~~ ✅ 2026-07-19実装(前半・後半とも完了)
3. ~~**F-3(構造的型付けの範囲)+ F-4(`!`→`?`)**~~ ✅ 2026-07-19実装
4. ~~**F-6(narrowing拡張)**~~ ✅ 2026-07-19実装(フィールドパス・`&&`・`!`/`||`のド・モルガン)
5. ~~**F-12(測定)**~~ ✅ 2026-07-20実施(第1・第2ラウンド。`bench/`・docs/benchmark-2026-07.md)
6. ~~**F-13(ツールチェーン契約)**~~ ✅ 2026-07-20実装(前半・後半とも完了)
7. ~~**F-7〜F-11(討議項目の残り)**~~ ✅ 2026-07-20実装(F-5も含め討議項目は全て決着)
8. ~~**F-14(mesh/io + mesh/json)**~~ ✅ 2026-07-20実装
9. ~~**F-15(mesh test --json)**~~ ✅ 2026-07-20実装。F節(F-1〜F-15)は全項目決着
10. 残るはC-6の続き(環境別モジュール・`mesh/http`)。F-14実装メモ(障害分離)がその着手時の参照先

## H. TS不満調査からの追加討議項目(2026-07-21追記)

> 「フロント/バック両方書きたいならTSでいいのでは?」という問いに対し、実際にTS/Reactへの
> 不満からMoonBit等へ移行した記事群(mizchi氏・lehti氏ら)を広く調査([related-languages.md](related-languages.md)参照)。
> critique-2026-07.md(B-5-2)が既に指摘していた2点が、その後の討議に一度も上がらないまま
> 未決着だったことが判明したため、ここに討議項目として起こす。

### H-1. `any` 型の扱い — ✅ 決着(2026-07-21): 選択肢(a)完全撤去を採用

critique-2026-07.md(B-5-2)の指摘: 「TSでよくない?」への3つの答え(①リークが構文的に
書けない並行、②不在/closedを無視できない強制、③抜け穴の無さ)のうち、**③はMeshの`any`型の
存在によって毀損されている**(TSの`as any`と同じ穴)。`src/checker/*.ts`を確認すると、
関数呼び出し・二項演算・インデックス・チャネル操作など広範囲で`t.kind === "any"`が
型検査をバイパスしており、TSの`any`と構造的に同じ効き方をする([features.md:63](features.md)は
「脱出口。乱用しない」という運用規約があるのみで、言語機構としての歯止めは無い)。

- 選択肢(a): `any`を完全撤去。用途(空配列の暫定型付け等)は他の機構(型注釈必須化・
  ジェネリクス)で代替できないか検討
- 選択肢(b): 用途をJS相互運用境界(Q2, npm interop)専用に限定し、ユーザーコードの通常の
  型検査経路からは到達不能にする
- 外部記事の裏付け: [Why TypeScript Won't Save You](https://cekrem.github.io/posts/why-typescript-wont-save-you/)
  「You're only as safe as your weakest `any`」

**実装メモ(2026-07-21)**: 実際に手を動かして穴を再現してから着手 — `x: any = 5` からの
型不整合な演算、`mut arr := []` への型混在push、どちらもコンパイルエラー無しで通ることを
実測確認した。`src/checker/*.ts`の`ANY`返却箇所を全数調査した結果、ほぼ全てが`error()`
呼び出し後のエラー回復(既にエラー報告済みなので無害)で、**唯一の無音の穴**は空配列
リテラル(`expr.elems.length === 0`)の型推論だった。よって:
- `resolveType`の`"any"`ケースを撤去し、書かれたら`any-type-removed`エラーにした
  (内部の`ANY`センチネル自体は残すが、もうユーザー構文からは到達できない)
- `containsAny(t)`(型を再帰的に走査し`any`混入を検出するヘルパ、`types.ts`)を新設し、
  `:=`(ローカルの`shortVarDecl`・トップレベル定数)で束縛の型を確定させる直前にだけ呼ぶ
  `cannot-infer-type`エラーとした。`typedVarDecl`・関数引数・struct リテラルのフィールドの
  ような「既存の型と照合するだけ」の文脈は対象外(`assignable()`の配列要素any許容ルールは
  維持したままなので`xs: Todo[] = []`等は今まで通り動く)
- 二重エラー防止: 値の評価中に既に`error()`が呼ばれていれば(未定義変数・判別可能unionの
  不一致等)`containsAny`チェック自体をスキップする(診断件数の差分で検出)。そうしないと
  既存のエラーに`cannot-infer-type`が便乗して二重報告になる
- テスト7件追加(checker: any-type-removed・cannot-infer-type(ローカル/トップレベル)・
  二重報告防止・既存の文脈つきパターンが壊れていないことの確認・予約名の維持)

### H-2. API境界の検証つきデコード(P6の実質化) — ✅ 決着(2026-07-21): 手書き用ヘルパー + json struct自動生成の両方を実装

critique-2026-07.md(B-5-2)の指摘: 「P6(フロント/バック型共有)は現状空手形。型共有だけなら
TSが既にやっている。**API境界で型が検証される**まで行って初めて存在理由」。
`mesh/json`の`json.parse`は`json.Value | error`を返す(無検証の`as User`キャストは
構造的に書けない)ところまでは実装済み([features.md:297](features.md))だが、
`json.Value`から具体的な`struct`へ**検証つきで変換する**手段(Goの`encoding/json`
Unmarshal相当、union路線なら`T | error`を返すデコーダ)がまだ無い。ここが埋まって
初めて「TSは型を共有しているだけ、Meshは境界で検証もする」という主張が成立する。

- 外部記事の裏付け: [Why TypeScript Won't Save You](https://cekrem.github.io/posts/why-typescript-wont-save-you/)
  — `return await response.json()`を`User`型として無検証で返す実例を「偽りの安全感」の
  典型例として批判している
- 着手順としてはC-6(環境別モジュール・`mesh/http`)より前に置く価値がある
  (HTTP自体より先にJSONデコードの安全性を固める方が土台として筋が良い)

**討議の経緯(2026-07-21)**: 当初A案(手書きデコーダ+ヘルパー関数のみ)を提案したが、
kanayamaから「他言語は自動でやっているはず」との指摘でB案(struct単位の自動生成)を採用。
ただしMeshのジェネリクスは`T`のフィールドに触れない設計(意図的な制約)なので、
`fn decode<T>(v: json.Value) T | error`という本当に汎用的なデコーダはユーザーコードでは
書けない。新しい型引数構文(`decode<User>(v)`)も増やさず、既存の「宣言前のマーカー」
パターン(`error struct`と同型)を再利用する形に決定。

**実装メモ**:
- ヘルパー関数(`mesh/json`に追加、A案はこれの単体利用として引き続き有効):
  `json.field(v, key) json.Value | error`(欠損/非objectはerror)・
  `json.optField(v, key) json.Value | none`(欠損/null→none、errorにしない)・
  `json.asString/asInt/asFloat/asBool(v) T | error`・`json.asArray(v) json.Value[] | error`。
  `asInt`は小数を丸めずerrorにする(F-10の精度方針を踏襲)
- 自動生成(`json struct X { ... }`): 新しい生JS文字列を書くのではなく、**Meshの構文レベルの
  AST(Stmt/Expr)をfield形状から合成し、通常のFnDeclとしてprogram.fnsへ追加する**方式を採用
  (`src/json-decode.ts`、`compiler.ts`のparse直後・check直前に配線)。手書きデコーダと
  全く同じ形(`json.field`/`json.asXxx`を`?`で繋ぐ)のコードを機械的に組み立てるので、
  以降のcheck/codegenは無改造のまま流用でき、生成コードにもバグがあれば普通に型検査で捕まる
- 対応範囲(v1): `int/float/string/bool`・**同一ファイル内**の他の`json struct`(ネスト)・
  それらの配列・`T | none`(欠損/null→none)。それ以外(素のstruct・map・一般union等)は
  合成時に該当フィールドを指してエラーにし、手書きデコーダへ誘導する。ネスト参照が
  ファイルをまたぐケースは対象外(v1制約、必要なら手書きで対応)
- `json type`(union)は意図的に非対応 — メンバー選択ロジックが要り自動導出できないため、
  専用のエラーで手書きデコーダへ誘導する
- `export json struct`は生成された`decode<Name>`も一緒にexportされる(他パッケージから
  `pkg.decodeX(v)`で呼べる。手動検証済み)
- テスト12件追加(checker 6件・e2e 6件: フラット・ネスト+配列+optionalの組み合わせ・
  各種エラー(欠損/型不一致/非object/サポート外フィールド/import漏れ/json type)・
  整数フィールドの精度検査・複数パッケージでのexport)

## I. C-6続き: 環境別モジュール・mesh/http — ✅ 決着(2026-07-21): サーバー専用・生ハンドラのみのv1を実装

H-1・H-2が決着し、todo.mdの「次の一手」順どおりkanayamaと討議して着手。C-6のコア
(importしたモジュールから環境をコンパイル時に自動判別する仕組み。`mesh/http`と`mesh/dom`の
同居はコンパイルエラー)は2026-07-19に既に決定済みだったので、今回は`mesh/http`の**中身**
だけを設計した。DOM側(未決Q3=フロントエンドの形)は今回もスコープ外のまま
(`mesh/http`はサーバー専用でQ3を必要としない)。

**討議の経緯**: まずAPIの形を2案提示——(1) Go `net/http`直訳(生ハンドラ1本+
`http.listen(addr, handler)`。ルーティングはユーザーが`req.path`/`req.method`を見て
自分でif分岐)、(2) 軽量ルーター内蔵(`http.handle(path, method, handler)`を複数回登録し
`http.listen(addr)`がディスパッチ)。kanayamaは「あとで積めるなら」の条件で(1)を選択。
さらにkanayamaからEchoの`e.GET(path, handler)`スタイル(メソッド別登録関数)が読みやすいと
いう感想があり、「これはフレームワークに任せるべきか」という問いが出た——回答は**任せるべき
ではない**: Meshにはまだサードパーティパッケージのエコシステムが無く(npm相互運用の深さ=
Q2が未決着のまま)、「フレームワークに任せる」という選択肢が実質存在しない。Go自身も
`net/http`が素のハンドラのみだった反省からGo 1.22で`mux.HandleFunc("GET /users/{id}", ...)`
という**メソッド付きパターンをstdlibに取り込んだ**前例があり、Meshも同じ道を辿るのが自然。
結論: **v1は(1)のまま実装し、v2として`http.get`/`http.post`/`http.put`/`http.delete`
(Echoスタイルのメソッド別登録+`http.listen(addr)`によるディスパッチ)を`mesh/http`自身に
将来追加する**——別パッケージ・別フレームワークへは切り出さない。パスパラメータ
(`/users/:id`)の抽出方式はv2着手時に別途詰める。

**v1の型**:
```
struct Request { method: string, path: string, query: string, headers: map<string, string>, body: string }
struct Response { status: int, body: string, headers: map<string, string> }
fn listen(addr: string, handler: fn(Request) Response) none | error
```
`path`はクエリを含まないURLパスのみ、`query`は`?`以降の生文字列(未パース、無ければ空文字列)。
`Response`の`headers`はstruct literalの必須フィールドなので、カスタムヘッダが無い場合でも
毎回`map<string, string>{}`を書く必要がある——v1では割り切り、利便関数(`http.text`等)は
追加しなかった(v2のルーティング糖衣とセットで検討する候補として残す)。

**`listen`は「サーバーが止まるまで待つ」関数ではない**: bindが成功したらほぼ即座に`none`を
返す。プログラムがそのまま動き続けるのは、listen中のソケットがNodeのイベントループを
生かし続けるため(`app.listen(3000)`が典型のExpress等と同じ構図)——`mesh/http`自体に
「待つ」ための新しい機構(`Server`ハンドルや`close()`)は追加していない。テストも子プロセスを
外側からkillする方式にしたため、そもそも`close()`が要らなかった(下記テスト戦略参照)。

**障害分離(F-14実装メモの初適用)**: requirements.md 5.5の方針どおり、1リクエストの
ハンドラが`panic`してもプロセス全体を落とさない——`__httpDispatch`(runtime.ts)がハンドラ
呼び出しを`try/catch`し、失敗時はサーバー側ログにだけ詳細を出し(`console.error`)、
クライアントには内部情報を含まない汎用の`500`だけを返す。手動検証(curlで`/boom`
エンドポイントを叩き、panicの直後に別エンドポイントへ再度リクエストして生存を確認)と
e2eテストの両方で、パニック後もサーバーが他のリクエストへ応答し続けることを確認した。
Mesh言語自体に`panic()`/`recover()`は追加していない(Go `net/http`が内部でrecoverするのと
同じ構図——分離はstdlib実装者の責任として1回書けばよい)。

**アーキテクチャ**: mesh/io・mesh/jsonと同じ「組み込みパッケージ」方式(`.mesh`ソース無し)。
`src/stdlib.ts`の`BUILTIN_PACKAGES`マップへ型シグネチャを追加するだけで、checker側は
一切の特別扱いなしにテーブル駆動で拾う(`checker/modules.ts`が汎用的にこのマップを走査する
実装だったため、mesh/http追加にあたりstdlib.ts/runtime.ts以外の変更は不要だった)。
実体(`http$listen`等)は`runtime.ts`のPRELUDEに素のJSとして埋め込み、`node:http`への
動的importでNode専用の依存を隔離する(mesh/ioの`readFile`と同じパターン。ブラウザ実行では
`error`に落ちる)。

**テスト戦略**: 既存のe2eヘルパー(`spawnSync`で子プロセスの終了〈exit 0〉を待つ)は使えない
——listen中のソケットがイベントループを生かし続けるため、サーバーを起動するプログラムは
自然には終了しない。代わりに`tests/http.test.ts`を新設し、`node:child_process`の非同期
`spawn`で子プロセスを起動したまま残し(`afterEach`で必ずkill)、ポートへの`fetch`が
成功するまで軽くポーリングしてから実ネットワーク越しにリクエストしてアサーションする方式にした。
`mesh/json`の`json struct`デコードと組み合わせたPOSTボディの検証つきデコードも
このテストで確認済み(パス分岐・クエリ文字列・リクエスト/レスポンスヘッダ・POSTボディの
json structデコード・障害分離・ポート二重bindのerror化)。

**カードのゲート化**: H-2の「検証つきJSONデコード」節は(隣接するF-14のmesh/json節も
常時収録のため一貫性を優先し)`mesh card --for`のサブセット対象外(=常に収録)にしたが、
今回`mesh/http`節を追加した結果、何も機能を使わない最小プログラムでもサブセットカードが
フルカードの60%を超えてしまった(60%比率のCI回帰テストで検出)。そのため`mesh/http`節は
`card-subset.ts`の`FEATURE_HEADINGS`/`FEATURE_PATTERNS`でゲート対象にし
(`import "mesh/http"`検出時のみ収録)、60%比率を再びクリアした。

**副産物**: 既存の回帰テスト2件(checker.test.ts・e2e.test.ts双方にあった「mesh/io,
mesh/json以外のmesh/\*は未実装」という趣旨のテスト)が、まさにmesh/httpを未実装の代表例として
名指ししていたため、mesh/http実装により意味が反転し失格(falseになるべき箇所がtrueになる)
することを発見・修正。対象を`mesh/dom`(今も本当に未実装)に差し替えた。

**マージ前の`/code-review`で発見・追加修正(`ada617c`)**: 今回のセッションから「マージ前に
`/code-review <PR> --comment`を必ず通す」フックを新設し、その初回適用で実際に1件の
高信頼度の指摘を拾った——`__httpReadBody`がリクエストボディを無制限にメモリへ溜め込んでおり、
1リクエストが巨大なボディを送るだけでプロセス全体をメモリ枯渇で落とせた。ハンドラのpanic
だけを隔離しても「1リクエストがサーバー全体を道連れにしない」という、このPR自身が謳って
テストもしている約束を守れていなかった——`mesh/http`はネットワーク越しの未信頼入力を
直接扱う最初の組み込みパッケージであり(mesh/io・mesh/jsonはローカル/既にメモリ上の入力しか
扱わない)、既存の許容リスクとの整合性論法も効かないという指摘だった。10MiB固定の上限を
追加し、超過時は専用の`__HttpBodyTooLarge`でrejectして`413`にすることで、panicと同じ
「1リクエストだけ隔離する」パターンに合流させた。実装時、`req.destroy()`で即座に
切断すると413のレスポンス自体がクライアントに届かず`ECONNRESET`になる罠を踏んだため
(手動curl検証で発覚)、`req.pause()`で読み取りだけ止め、`Connection: close`ヘッダを
付けたレスポンスを書き終えてからNodeにソケットを閉じさせる方式に直した。もう1件
(catch節自身の`res.writeHead`/`res.end`がアボート済み接続で同期的にthrowしプロセスが
落ちうるのでは、という指摘)は別のOpusエージェントが実機検証(Node実装コードリーディング+
panic×大量abort×タイミングをずらした1200回のストレス試験)の末に「Node実装ではdestroyed
socketへの書き込みは無害なno-op」と反証し、却下した。

テスト12件追加(checker 6件・e2e 6件、うち1件はcode review指摘の413回帰テスト)。
todo.md「次の一手」に残る大きな討議項目は、これでC-6(コア+mesh/http v1)まで完了。
次点は同todo.md記載のとおり、言語カード実証実験の継続またはRust移植の開始

## J. json struct のエンコード方向(H-2の裏返し) — ✅ 決着・実装済み(2026-07-24): 選択肢(a)を採用

`demo/todo-api/main.mesh`実装中に発覚: H-2で`json struct X {...}`から自動生成されるのは
**デコード方向**(`decode<X>(v: json.Value) X | error`)だけで、**エンコード方向**
(`X` → `json.Value`/JSON文字列)は無い。実際にデモでは`Todo` → `json.Value`変換
(`toJson()`メソッド)を毎回手で組み立てる必要があり、同じ形のボイラープレートが
3箇所(`toJson`・`jsonResponse`・`errorResponse`)に発生した。

他言語比較: Go(`encoding/json`のMarshal)・Rust(serdeのderive `Serialize`)は構造体
タグ/deriveでデコード・エンコード**両方向**を自動生成するのに対し、現状のMeshは
デコードのみ自動という非対称な状態——H-2時点では意識していなかった穴。

**討議の経緯(2026-07-24)**: 3つの選択肢を提示——(a) `encodeX(x: X) json.Value`という
対になる自由関数(`decodeX`と同じ命名規則・同じAST合成方式をそのまま流用できる)、
(b) レシーバメソッド`(x: X) toJson() json.Value`(struct宣言に「メソッドが後から自動で
生える」という新しい挙動が入り、「メソッドはユーザーが自分で書く」既存設計と整合しない
懸念があった)、(c) `json.Value`を経由せず`stringifyX(x: X) string`まで直接生成
(構成しやすさを失う代わりに最短化)。kanayamaと相談し(a)を採用。

**実装メモ**:
- `src/json-decode.ts`に`synthesizeJsonEncoders`(+ヘルパー群)を追加し、
  `compiler.ts`で`synthesizeJsonDecoders`の直後に呼ぶ。デコードと対称の対応範囲
  (`int/float/string/bool`・同一ファイル内の他の`json struct`・それらの配列・
  `T | none`)、対称のエラー(`json-struct-unsupported-field`/
  `json-struct-missing-import`)。エンコードは検証を伴わない(Mesh側は既に型付きなので
  失敗し得ない)ため戻り値は`json.Value | error`ではなく素の`json.Value`
- decode/encodeは同じ`isSimple`/`optionalInner`ヘルパーを共有するため対応範囲は完全に
  対称——compiler.tsでは`synthesizeJsonDecoders`が先に走るので、実運用上は
  「decodeが通ればencodeも必ず通る」(decode側のエラーが先に出る)。それでも
  `synthesizeJsonEncoders`自身は自己完結した検証を持つ(decode側の実装都合に依存しない
  設計、テストからも直接呼べる)
- **テスト**: `synthesizeJsonEncoders`単体の型検査(checker.test.ts 5件: フラット・
  ネスト+配列+optional・サポート外フィールドの拒否・import漏れの拒否・export越しの
  呼び出し)+ 実行結果(e2e.test.ts 5件: フラット・ネスト+配列+optional・
  配列要素がjson struct+配列自体optional・encode→decodeのラウンドトリップ・
  複数パッケージでのexport)。500件全件パス、`tsc --noEmit`クリーン、既存の全example
  (`defer_panic.mesh`の意図的なpanicを除く)を再実行し回帰無し
- **Rust移植版は対象外**: `rust/src/json_decode.rs`はTS版`json-decode.ts`の
  デコード部分を忠実に移植したものだが、今回追加したエンコード方向はまだ移植していない
  (`rust/`は並行して育てている移植版であり、TS側の新機能に即座に追随する義務は無い
  ——CLAUDE.md「Rust移植について」参照)。そのため`demo/todo-api/main.mesh`は
  意図的にこの新機能へ切り替えなかった——`json struct Todo`にして`encodeTodo`を
  使う版を試作したところ、Rust版が生成したJSには`encodeTodo`の**呼び出しだけ**が
  含まれ定義が無く、実行時`ReferenceError`になることを実機確認した(Rust版の
  `json_decode.rs`にエンコード合成が無いため)。デモの存在意義が「TS版・Rust版
  どちらでコンパイルしても同じ挙動」であることなので、この試作は取り消し、
  デモは既存の手書き`toJson()`のまま据え置いた
