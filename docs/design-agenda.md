# 設計アジェンダ — コンセプトメモとの突き合わせ(2026-07-17)

> [concept-memo.md](concept-memo.md)(別セッションの記録)と、このリポジトリの決定事項
> ([features.md](features.md) / [requirements.md](requirements.md))を突き合わせた結果。
> 3分類: **A. 一致**(追認) / **B. 衝突**(要決着) / **C. 新規アイデア**(未議論)

## A. 一致している(すでに両者で同じ結論)

| 項目 | 状態 |
|---|---|
| コアコンセプト(AIが書き・人が読む/一つの書き方/ローカル推論/修正を示すエラー/学習データ近さ) | requirements の P1〜P5 と同内容。メモの「型注釈=圧縮されたコンテキスト」という言語化は P3 の補強として採り入れたい |
| 波括弧デリミタ(インデント構文不採用) | v0 実装済み。メモの根拠「AIのdiff/部分編集に空白構文は脆い」は新しい論拠として記録価値あり |
| match式採用・switch不採用・網羅性チェック | 2026-07-17 に同結論で決定済み |
| enum不採用・union型採用 | 決定済み(ただしメモはさらに先へ → B-1, C-1) |
| 例外なし・エラーは値 | 決定済み(ただし表現形式が違う → B-2) |
| JSターゲット・フロント/バック型共有 | P6 と一致。APIコントラクト構想は C-7 へ |
| Go風並行処理・型付きchannel・関数の色問題の解消 | 決定済み(spawn)。「全関数async化」で実装済みのため、メモの懸念のうちCPS変換問題は既に回避されている |
| フォーマッタ一択・設定なし | mesh fmt として決定済み |
| 関数シグネチャの型注釈必須 | v0 実装済み(公開関数だけでなく全関数で必須。メモより厳しい側で一致) |
| コンパイラの構造化出力(JSON) | mesh check --json として決定済み |
| 並行≠並列の割り切り明記 | features.md に記載済み |

## B. 衝突している(要決着)

### B-1. 空値の設計 — ✅ 決着(2026-07-17): `T | none` を採用(union路線)

- **このセッションの決定**: `nil`→`null` 改名。空値は null 1つ。null は error/chan/配列/関数型に代入可
- **メモの決定**: null 無し。不在は `T | none` の union で表現し、match / if narrowing で安全に取り出す
- **論点**: null は「どこにでも入り得る値」なので、チェッカーは「null かもしれないのに使った」を
  検出できない(10億ドルの過ち)。`T | none` は不在を**型に**するので、取り出しに narrowing が
  強制され、未処理がコンパイルエラーになる。union+match+網羅性を採用済みの現在、
  `T | none` の方が一貫する
- **影響範囲**: 大。checker に narrowing(型の絞り込み)実装が必要。features.md の null 行の書き換え

### B-2. エラーの表現 — ✅ 決着(2026-07-17): `T | error` を採用(union路線)。多値戻りは廃止。`!`/`or` は union 上で維持

- **このセッションの決定**: Go流 `(T, error)` 多値戻り + `!` 伝播 + `or`(v0実装済み・E1/E2採用済み)
- **メモの方向**: Result的union `{ kind: "ok", value: T } | { kind: "err", error: E }` を返す
- **論点**: B-1 と本質は同じで、**「不在・失敗を『特別な値』で表すか『union型』で表すか」**という
  言語の背骨の選択。union路線なら エラー/不在/channelのclose(C-5)まで全部
  「union + match + 網羅性」の一つの原理で説明できる(概念の数が最小になる)。
  Go路線は学習データ・v0実装・既決定(E1/E2)との連続性が強み
- **`!`/`or` への影響**: union路線にしても `!`(Rustの `?` 相当)や `or` は定義し直せる。
  ただしメモは「見えない早期リターンの復活」として `?` 系そのものに慎重論を残している
  (E1採用の再確認が必要)
- **影響範囲**: 特大。エラー処理は言語の顔。決めたら仕様書に固定して以後動かさない

### B-3. select — ✅ 決着(2026-07-18): matchの見た目を踏襲した独立構文で実装

`select { v := <-ch1 => body  v := <-ch2 => body  _ => body }`。実装してみると
match の型パターンとは本質的に別物(「型が何か」ではなく「どの受信操作が先に終わったか」)と
判明したため、match を乗っ取らず独立構文にした(見た目のみ踏襲)。複数同時準備なら
擬似ランダム選択(Goと同じくcase飢餓を防ぐ)。送信ケースは無し(Meshのchannelは既定
無制限バッファのため送信不可の待ち合わせはほぼ無意味)。C-5と同時実装。テスト206件

### B-4. 宣言構文の流儀 — ✅ 決着(2026-07-17): `:=` 不変デフォルト + `mut`

決定内容: `x := 10` は不変束縛 / 可変は `mut count := 0` のみ / 引数は常に不変 /
C風forのヘッダ変数だけ暗黙可変(唯一の構造的例外) / v1の不変性は束縛レベル
(JSのconst相当。中身の深い不変性は将来検討)。キーワードは `var` 案も検討したが
`mut`(Rustの let mut 相当)に決定。`let` 単独案は「JSのlet(可変)と同字面で逆の意味」
になるため不採用。

- **このセッションの決定**: `:=` + シャドーイング禁止(実装済み)。const は不採用
- **メモの提案**: デフォルト不変 `let` + 可変明示 `mut`(レビューで mut だけ注視すればよい)
- **論点**: 「デフォルト不変」はレビュー・AI出力の安定に効く実績ある設計(Rust)。
  ただし宣言キーワードが増え、`:=` の簡潔さと衝突する。中間案もある:
  `:=` を不変束縛とし、可変にしたい変数だけ `mut x := 0` と書く(1キーワード追加で済む)
- **付随**: メモのコード例は TS 風味(`fn f(): T` のコロン戻り値、named args)で、
  現行のGo風味と表層構文の流儀が揺れている。B-1/B-2 の決着後に構文仕様書で統一する

### B-5. 型宣言の構文(追記・同日決着): `struct` / `type` の分離

union路線決定後、「`type Status = ...` と `type User { ... }` の2形態が紛らわしい」との
指摘(kanayama)から T1 を改訂。**データ定義は `struct User { ... }`、名前付け・unionは
`type X = ...`** に分離。意味論は構造的型付けで統一(キーワードの違いは読み手のための
表層区別であり、Goのような alias/新型の意味差は無い)。`type =` の右辺に裸の `{...}` は
エラーにして書き方を1通りに保つ。`{...}` 型式は union 内では有効(判別可能union C-1 への道)。

## C. 新規アイデア(このセッションでは未議論)

| # | アイデア | 一言評価 |
|---|---|---|
| C-1 | ~~**判別可能ユニオン**(タグ付きオブジェクトのunion)+ narrowing~~ | ✅ 決着・実装済み(2026-07-19)。
      `type X = { kind: "ok", ... } | { kind: "notFound" }` — union内だけで無名 `{...}` 型式が書ける
      (B-5の布石どおり)。構築は union自身の名前をstructリテラル名に流用(`X{kind: "ok", ...}`、
      フィールド集合で該当メンバーを特定)。narrowingは既存のmatch機構を拡張した部分構造パターン
      `{ kind: "ok" }` で行い、絞り込み後は普通のフィールドアクセス(新しい束縛構文は増やさない、
      P1)。この実装のために struct の同一性判定を名前ベース→全面的な構造的比較に変更した
      (design-agendaの「構造的型付け」項目を前倒しで実装)。自己参照(木構造・AST等)も同日中に
      追加実装 — structフィールド越しの参照はknot-tyingで解決。structを挟まない裸のunion同士の
      相互再帰(`type A = B | none` かつ `type B = A | error`)だけは、flatten時に相手の
      placeholderがまだ空で型情報が消える不具合を避けるため、意図的に`type alias cycle`のまま |
| C-2 | **matchガード**(`case x if x > 0`相当) | 小さい追加。match採用済みなので自然な拡張 |
| C-3 | **named args**(`createUser(name: "x", age: 3)`) | AIの引数順ミス対策として有効。呼び方が2通りになるならP1と緊張。「引数2個以上は必須」等の一律ルールなら整合 |
| C-4 | **let/mut(デフォルト不変)** | B-4 に統合 |
| C-5 | ~~**channelのclose設計**~~ | ✅ 決着・実装済み(2026-07-18)。`close(ch)` + `<-ch`は常に`T \| closed`。
      二重close・close済みへの送信はpanic。`closed`は`!`/`or`の対象に含めない(B-3と同時実装) |
| C-6 | **標準ライブラリの層分け**(core共通 / 環境別モジュール、importで環境をコンパイル時判別) | 「サーバーコードがdocumentを触るとコンパイルエラー」はAI-firstに強く効く。未決Q3(フロントエンドの形)の有力な骨格。
      **2026-07-19進捗**: 土台のモジュールシステムを実装(パッケージ=ディレクトリ・export可視性・
      `pkg.symbol`修飾。kanayamaとGo風を選択)。環境判別は「importしたモジュールから自動推定」方式に
      決定済み(`mesh/http`と`mesh/dom`の同居はエラー)。環境別モジュールの中身はQ3討議とセットで次段階 |
| C-7 | **APIコントラクトの言語ネイティブ化**(tRPC/Zod相当を言語で) | P6の最終形。モジュールシステム設計とセットで中期課題 |
| C-8 | **Promiseブリッジ/FFIの仕様化** | 未決Q2(npm相互運用)と同件。「境界でPromise→channel/多値、例外→エラー値、undefined→不在」を言語仕様として明文化する話 |
| C-9 | **UIイベントをchannelで扱う** | C-6/Q3とセットで検討。並行モデルとUIの統一感が出る面白い構想 |

## D. 推奨討議順

1. ~~B-1 + B-2(空値とエラーの統一モデル)~~ — ✅ **決着: union路線**(2026-07-17)。
   実用シナリオ比較(連鎖処理はほぼ同型 / 不在と失敗の区別 / うっかりミス表)の結果、
   「未処理・仕様変更漏れをコンパイラが捕まえる」を決め手に採用
2. **B-4(let/mut vs :=)+ 表層構文の流儀** — 背骨が決まれば見た目を統一できる
3. **C-3(named args)・C-2(matchガード)** — 小粒の採否
4. **E-1 → B-3 + C-5(channel露出の是非 → select/close などchannel仕様)** — 並行処理の完成。
   E-1(構造化並行に寄せるか)を先に決めてから select/close の詳細に入る
5. **C-6〜C-9(stdlib層分け・FFI・APIコントラクト)** — 未決Q2/Q3と統合して設計
6. メモの未決6「構文仕様の文書化」 — 上記が済んだら言語仕様書(spec)へ移行

## E. 類似言語調査からの論点(2026-07-18追記)

MoonBit調査([related-languages.md](related-languages.md))から出た討議事項。

### E-1. channelを露出すべきか — ✅ 決着(2026-07-18): 2段スコープ設計を採用(kanayama発案)

決定内容: channel は維持しつつ、**すべてのタスクに所有者を持たせる**。
`spawn` = 囲む関数が所有(関数を抜けるとき暗黙に待つ=リーク構文的に不可能)/
`detach` = プログラムが所有(呼び出し元は待たず、プログラム終了までに完了)。
発端は kanayama の「トランザクションの defer rollback のように、後片付けを自動保証
できないか」という提案。「waitの中でしかspawnできない」制約案(B案)より強い保証を、
既存コードを一切壊さず(checker無変更・codegenのみ)実現した。
Kotlin(GlobalScope非推奨)・Swift(Task.detached)・Python(TaskGroup)と同方向で、
エスケープハッチが `detach` として可視(grep可能)なのは `mut` と同じ思想。

- **現状のMesh**: Go風 `spawn`/`wait`/channel(実装済み)。残タスクは select(B-3)と容量指定
- **MoonBitの設計**: channel無し。タスクは task group 内でのみ生成でき、group は全タスク終了後に
  のみ返る(構造化並行)。孤児タスク(goroutine泄漏)が設計上発生せず、キャンセルも組み込み
- **論点**: B-3(select)や容量指定・close設計(C-5)に進む前に、「channelは言語の顔として
  残すか、構造化並行に寄せるか」を一度決める。Meshの `wait` ブロックはすでに構造化並行に
  半歩踏み出しており、折衷(waitを唯一のspawn許可スコープにする等)もあり得る。
  P1(書き方は一つ)とP5(学習データ近さ=Goのchannel資産)が衝突する構図

### E-2. AI向けレビュー支援ツールの優先度

- MoonBitは fmt(設定なし)と**言語組み込みスナップショットテスト**を早期整備。
  「AIが書いた差分を人がレビューしやすくする」効果が大きい
- Meshでは `mesh fmt` がTODO済み。スナップショットテストは未議論 → 採否を検討
- なお `mesh check --json`・全関数の型注釈必須は MoonBit と同方向で追認材料

## F. 批評ターン起点の再討議項目(2026-07-19追記)

批評ターン([critique-2026-07.md](critique-2026-07.md)。言語設計者レンズ+実務レンズの
2独立批評+内部批評の統合)から出た討議事項。**F-1〜F-4は過去の決定の再討議**であり、
「決めたら動かさない」原則の例外として、批評で新しい根拠(実測・学習データとの負転移)が
出たものに限って再開する。

### 討議が必要(設計判断)

| # | 論点 | 一言評価 |
|---|---|---|
| F-1 | **関数型注釈 → 最小ジェネリクス**(`f: fn(int) int` / `fn first<T>(...)`)| **前半(関数型注釈・単相)✅ 2026-07-19実装**。
      構文は関数宣言と同じ読み(unionは戻り値側に束縛・関数自体のunionは括弧・パラメータ名なし)。
      ユーザー定義の高階関数(apply/retryInt/makeAdder/structフィールド/chan要素)が書けることを確認。
      **後半(ジェネリクス)は未着手** — 成功判定「contains等の組み込み特権をstdlib通常関数に降格」は
      後半で達成する。型推論の範囲(明示instantiation要否)等を要討議 |
| F-2 | **エラー設計の和解** | **前半(コンテキスト付加形)✅ 2026-07-19実装**: `f() ? "line ${i}"` — 失敗時だけ文脈を評価し
      `error("文脈: 元メッセージ")` として伝播(noneも`error("文脈")`に昇格)。
      **後半(構造化エラー=判別可能unionの伝播対象化)は未着手** — 「どのメンバーが成功/失敗か」の
      判定規則に難しいケース(`User | none`のstructは成功側)があり、別途じっくり討議 |
| F-3 | ~~**構造的型付けの範囲の再討議**~~ | ✅ 決着・実装済み(2026-07-19、kanayama承認)。**名前的に巻き戻し**:
      名前付きstruct同士は名前で判定(Meters→Dollarsはコンパイルエラーに)。無名`{...}`メンバーが
      絡む比較だけ構造的(名前が無いため。判別可能unionの機能・互換は全て維持)。
      検討した代替案 opt-in `distinct struct` は「安全がオプトインになる逆向き設計」を理由に不採用 |
| F-4 | ~~**伝播記号 `!` → `?` への改名**~~ | ✅ 決着・実装済み(2026-07-19、kanayama承認)。後置`?`に改名
      (Rustと記号・意味が完全一致)。旧`!`は「postfix '!' was renamed — use '?'」の誘導つき
      パースエラー。前置`!`(否定)との同字面衝突も解消 |
| F-5 | ~~**`or` のerror無音吸収**~~ | ✅ 決着・実装済み(2026-07-19、kanayama承認: **Go式の明示性**)。
      素の`or 式`はnone専用。errorを含むunionには束縛形が必須 — `or e => 式`(使う)/
      `or _ => 式`(捨てる痕跡を字面に残す。Goの`v, _ :=`のunion路線翻訳)。
      「自動でログ出力」案はP2違反・出力先はアプリの都合・テスト汚染を理由に不採用 |
| F-6 | ~~**narrowing拡張**~~ | ✅ 決着・実装済み(2026-07-19)。条件式から then側/else側で成り立つ事実(パス→絞り込み型)を
      再帰的に集める方式で一括対応: (1) フィールドパス(`if n.next is none`、rootが不変変数なら任意の深さ)、
      (2) `&&`連結(左の`is`が右辺の式とthen節の両方に効く)、(3) `!`/`||`のド・モルガン(`!(a is none)`、
      `a is none || b is none { return }`後は両方絞り込み済み)。フィールドへの代入(`n.next = ...`は
      いつでも許される)は古い事実を無効化してから型検査するので健全性は保つ。`match`の対象もついでに
      同じ仕組み(安定パス)に統一し、`match n.next { ... }`も絞り込めるようにした |
| F-7 | **判別可能unionのタグ必須化** — フィールド集合推論の遠隔作用(メンバー追加で離れたリテラルが壊れる)を、タグ(kind等のリテラル型フィールド)必須で解消 | 内部批評の対案。P3(局所で読める)の回復 |
| F-8 | **transform/map の再々検討** — 型位置`map<`と式位置`map(`は字句区別可能なので文脈依存キーワード化 | 「AIが必ず書き間違える名前」のコストは一貫性のコストより大きいという指摘 |
| F-9 | **小さな一貫性の穴の掃除** — 空の型付き配列2通り(`Todo[]{}` vs `xs: T[] = []`)/ `+=`不在なのに`++`はある / トップレベル定数の禁欲 / `get(arr, i) → T | none` | P1違反の自家製箇所。個別は小さいがまとめて決める |
| F-10 | **int 53bit のsafe-integer検査** — `9007199254740992 + 1`が無音で+1を失う。panic層(範囲外・ゼロ除算と同じ思想)に追加 | 「バグは即停止」哲学の未適用箇所。実装は安い |
| F-11 | **chan既定無制限バッファの説明責任** — 「goroutineリークは死んだがメッセージリークは生きている」。仕様として文書化 or 既定を有限に | C-5決定の補強討議。全関数async化のinterleaving意味論のカード明記もセット |

### 実装・計測タスク(討議より実行が主)

| # | 項目 | 一言評価 |
|---|---|---|
| F-12 | **対TS/Go比較ベンチマーク** — 同一タスクをMesh/TS/Goで白紙AIに書かせ、一発成功率・修正往復数・diff一貫性を測定 | **核心仮説「言語設計がAIの正答率を左右する」が未測定**という最大リスクへの回答。カード実験の進化形 |
| F-13 | **ツールチェーン契約の強化** — 診断にエラーコード+機械適用可能な`fix`パッチ、`mesh explain <code>`、サブセットカード、カード完全性の逆方向検査(実装がカードからはみ出したらCIで落とす。`s[0]`未記載が実例) | 3視点一致の「最大の独自資産」への投資 |
| F-14 | **mesh/io + mesh/json**(C-6の続き)— 実アプリ距離の最短経路。json.Valueは自己参照判別可能unionのショーケース | 「入出力ゼロ」問題。IOのAPI設計にはF-1(関数型注釈)が先行依存 |
| F-15 | **mesh test --json** — 「型は通ったが挙動が違う」を機械可読で返し、生成→検証ループを完結させる | 既存TODOの優先度格上げ |

### G. 推奨討議順(F節)

1. **F-1(関数型注釈)** — 多くの項目(sortBy・IOコールバック・F-14)の前提。単相なら小さい
2. **F-2(エラー設計)+ F-4(`!`→`?`)** — 同じ演算子を触るので同時に決める
3. **F-3(構造的型付けの範囲)** — 巻き戻すなら早いほど安い
4. ~~**F-6(narrowing拡張)**~~ ✅ 2026-07-19実装(フィールドパス・`&&`・`!`/`||`のド・モルガン)
5. **F-12(測定)** — F-1/F-2/F-6が入った後に回すと、改善の効果測定を兼ねられる
6. 残り(F-5, F-7〜F-11, F-13〜F-15)は上記と並行または後続
