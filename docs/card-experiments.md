# 言語カード実証実験ログ

> 白紙のAI(この会話の記憶なし・リポジトリ閲覧禁止)に `mesh card` の出力だけを渡し、
> 実タスクを `mesh check --json` ループで書かせて、往復数と「詰まり」を記録する。
> 目的: `mesh check` のテストでは気づけない実用上の穴(実装 or カード)を炙り出す。
> 手法の詳細はメモリ [[mesh-card-experiment]] に記録。

## 往復数の推移

| 回 | 日付 | 題材 | 往復数 | 主な発見 |
|---|---|---|---|---|
| 1 | 2026-07-18 | TODO管理 | **6** | 空の型付き配列が書けない(実装の穴)→ `Todo[]{}` 追加 |
| 2 | 2026-07-18 | TODO管理(同一) | **1** | 空配列は解消。カード記述漏れ4件が「賭け」として報告 → 追記 |
| 3 | 2026-07-18 | 単語頻度カウンタ | **1** | 別題材でも一発成功。イディオム未掲載3件を報告 |
| 4 | 2026-07-18 | 単語頻度(第3回と同一) | **1** | 型注釈導入後の効果測定。第3回でフラグ回避した不在アキュムレータを `mut best: string \| none = none` で素直に記述。残る指摘は設計選択のみ(穴ではない) |
| 5 | 2026-07-19 | 簡易電卓(式評価) | **1** | 判別可能union実装後、初の別題材。**一発成功**。ただし判別可能unionは使われず
      (トークン列に分岐する形が無いタスクだったため=妥当な設計判断)、if/elseの両ブランチ絞り込みの
      実例がカードに無いと指摘(記述漏れ) |
| 6 | 2026-07-19 | 括弧つき式評価(再帰下降パーサ) | **2** | if/else記述追記後。判別可能union AST
      (`{kind:"num",...} \| {kind:"bin",...,left:Expr,right:Expr}`)を意図的に試させたところ
      `type alias cycle` で1往復失敗 → 2往復目でAST自体を捨てて直接評価に切り替え成功。
      「機能が無いと分かった時にどう回避するか」まで観測できた貴重な失敗ケース |
| 7 | 2026-07-19 | 並行バッチ処理(wait/spawn/detach) | **3** | 初めて並行処理層を経験させた。
      **新しい実装の穴を発見**: `chan<int>[]`(chan/mapなど総称型を要素とする配列型)がパースエラー
      になる(`expected '=' in typed declaration ... but got '['`)。AIは配列を諦め、受取口5つを
      個別変数`t1`〜`t5`に持たせて回避し3往復目で成功。wait/spawn/detachそのものは一発で正しく書けた |
| 8 | 2026-07-19 | 注文集計(struct メソッド + filter/transform/reduce) | **2** | 初めて標準ライブラリの
      高階関数とstructメソッドを組み合わせた題材。**新しい実装の穴を発見**: 複数行の配列リテラル
      (`[\n  Order{...},\n  Order{...}\n]`)が末尾カンマ無しだと`expected ',' between array
      elements, but got ';'`で落ちる。struct/mapリテラルは既にASI由来のセミコロンを読み飛ばして
      いるのに、素の配列リテラルだけ対応していなかったのが原因。1往復目で失敗→末尾カンマを足して
      2往復目で成功。filter/transform/reduceとstructメソッドの組み合わせ自体は完全に一発で正しく
      書けた(named fnとinlineクロージャを両方使い分けるなど、カードの説明通りの理解) |
| 9 | 2026-07-19 | ログイベント分類(LOGIN/LOGOUT/ERROR/unknown) | **1** | **完全一発成功・エラー0件**。
      判別可能unionを使う価値が本当にある題材(4種の相互排他な形、非再帰)を与えたところ、
      **AIは自発的に判別可能unionを選んで正しく使い切った**(第5回は「使わない」判断、
      第6回は自己参照で失敗——今回初めて「使うべき時にちゃんと使えて成功する」ケースを確認できた) |
| 10 | 2026-07-19 | 成績集計(sort/contains/indexOf/keys/values の実地利用) | **1** | **完全一発成功・
      エラー0件**。これまで未検証だった標準ライブラリ層(`sort`/`contains`/`indexOf`/`keys`/`values`)
      を組み合わせた題材でも穴・記述漏れ無し。`indexOf`の`int \| none`をif/elseで絞り込む、
      `join`のため`str()`で`int[]`→`string[]`変換、`m[k] or 0`でmap読みを平らにする、といった
      既存イディオムの複合適用がすべて一発で決まった |

第1回→第2回で `Todo[]{}` 実装+カード追記により 6→1 往復。第3回は別題材でも1往復。
第4回は型注釈導入後に第3回と同一題材で再測定 — 往復数は1のまま**だがコード品質が改善**(フラグ変数 →
本物の absence handling)。「穴を埋めた効果」は往復数でなく生成コードの質に現れた、という学び。
第5回(判別可能union実装後の初回験)も1往復で成功。新機能を意図的に見せずタスクだけ与えた結果、
**AIは判別可能unionを使わないという合理的な設計判断をした**(理由付きで報告)——「機能が存在する」
ことと「その機能がこのタスクに適切」は別問題、というのは実証実験として貴重な区別。
第6回はAST分岐が要る題材で、AIに判別可能unionを意図的に試させた。カードが予告している通り
`type alias cycle`で失敗し、AIはそこで**カードが提案する「名前付き再帰struct」には進まず、
木構造自体を持たない直接評価へ設計を変更**した(2往復目で成功)。この回避も理にかなってはいるが、
カードの回避策(「名前付き再帰struct、または個別のoptionalフィールド」)が実地では選ばれなかった、
という点は次の改善候補として記録に値する。
第7回は初めて並行処理(wait/spawn/detach)を経験させた。基本パターン自体は一発で正しく書けたが、
**`chan<int>[]`(総称型を要素にする配列型)がパースできない**という新しい実装の穴が見つかった。
複数の受取口を配列でまとめて`for`で回収する、という並行処理では自然にやりたくなる書き方が
今のMeshでは表現できず、個別変数への分解という(スケールしない)回避策を強いられている。
第8回は初めて標準ライブラリの高階関数(filter/transform/reduce)とstructメソッドを組み合わせた。
機能そのものの理解は完璧だったが、**複数行の配列リテラルが末尾カンマ無しだと壊れる**という
新しい実装の穴が見つかった——struct/mapリテラルは既にASI由来のセミコロンを読み飛ばしているのに、
素の配列リテラル(`[...]`)だけ対応していなかったのが原因(構造体要素に限らず、複数行の`int`配列
でも同様に再現)。
第9回は判別可能unionが本当に活きる題材(LOGIN/LOGOUT/ERROR/unknownという4種の相互排他な形、
非再帰)を用意し、機能の存在をヒントせずタスクだけ渡した。結果は**完全な一発成功・エラー0件**——
AIは自発的に判別可能unionを選び、「4つの相互排他な形・非再帰だから`type alias cycle`の罠に
当たらない」と正確に理由づけて報告した。第5回(妥当な不使用判断)・第6回(自己参照で失敗)に続き、
「使うべき時にちゃんと選んで正しく使い切れる」ケースを初めて確認できた、判別可能union検証の
仕上げといえる回。match をvoidを返す文(`push`呼び出し)だけのアームで**文として**使うパターンも
問題なく通った。
第10回は標準ライブラリの`sort`/`contains`/`indexOf`/`keys`/`values`を初めてまとめて実地で使わせた
(成績表を名前順にソートして表示・スコアを昇順ソートして`join`・`contains`/`indexOf`で検索)。
これも**完全一発成功・エラー0件**——`indexOf`の`int | none`をif/elseで絞り込む・`join`のために
`str()`で`int[]`を`string[]`へ変換する・`m[k] or 0`でmap読みを平らにする、という複数のイディオムを
同時に組み合わせても破綻しなかった。第1回(6往復)からの改善が標準ライブラリ層でも定着している
ことを裏付ける回。

## 発見した穴の分類

### A. 実装の穴(機能が無い/不足)
- **空の型付き配列**(第1回)→ 解消: `Todo[]{}` / `int[]{...}` / 多次元 `int[][]{}` を実装
- **不在から始まる可変アキュムレータ `mut best := none`**(第3回)→ **解消(2026-07-18)**。
  型注釈つき宣言 `mut best: string | none = none` を実装(対策案a)。あわせて空配列も
  `xs: Todo[] = []` で書けるようになった(`any[]` を型付き配列へ入れられるよう assignable を緩和)。
  つまり第1回の空配列問題と第3回の不在アキュムレータ問題が、型注釈宣言1つで両方解決した。
- **`chan<int>[]` / `map<K,V>[]` がパースできない**(第7回)→ **解消(2026-07-19)**。
  `parser.ts`の`parseSingleType()`を`parseTypeAtom()`(chan/map/name/リテラル/noneを1つ読む)
  + `parseArraySuffix()`(`[]`サフィックスを読む)に分割し、配列サフィックスをどのatomにも
  一律に効くようにした。`chan<int>[]`・`map<string,int>[]`・`chan<int>[][]`で解消を確認、
  fan-out/fan-inパターン(配列で受取口をまとめて`for`で回収・合計55)もe2eテスト化
- **複数行の配列リテラルが末尾カンマ無しだと壊れる**(第8回)→ **解消(2026-07-19)**。
  素の配列リテラル(`case "[":`)だけ`skipSemis()`を呼んでおらず、struct/mapリテラルや型付き
  配列リテラル(`Todo[]{...}`)は既にASI由来のセミコロンを読み飛ばしていたのに素の`[...]`だけ
  対応漏れだった。同じ`skipSemis()`+`match(",")`(任意)のパターンに揃えて解消
  (`int`要素・struct要素どちらの複数行配列でも末尾カンマ無しで動作確認・parser/e2eテスト追加)

### B. カードの記述漏れ(実装は正しく動く)→ 追記で解消済み
- 配列/map/struct/chan は参照値・関数に渡すと共有(第2回)
- グローバル変数なし、共有状態は引数で渡す(第2回)
- `:=` はリテラル型を string に widening、`mut s := "a"` は再代入可(第2回)
- 前置 `!`(否定)が存在する。後置 `x!`(伝播)とは別物(第2回)※機能はあったが使われず冗長化していた
- `print` はスペース区切り+末尾改行(第2回)
- error のメッセージは `is error` で絞ってから `${e}` で補間(第1回)
- push は none を返す・値として使えない(第1回)
- `len()` は map のキー数にも使える(第3回)
- map の読み書き更新イディオム `m[k] = (m[k] or 0) + 1`(第3回、+= も comma-ok も無いので明示が要る)
- `if x is error { ... } else { ... }` で else 側も自動的に絞り込まれる実例がカードに無い(第5回)。
  早期return形しか載っておらず、AIは「たぶん対称なはず」と推測で書いて正解した(実装は元々対応済み)。
  **→ 2026-07-19、カードに追記して解消済み**
- `for` 内の `break` が `is` narrowingを続けさせる実例がカードに無い(第6回)。`if op is none { break }`
  の後で `op` を普通の値として使えるかは未確認のまま推測で書いて正解(returnと同じ扱いで実装済み)
- `is` を `||`/`&&` と組み合わせた複合条件でnarrowingが効くかカードに書いていない(第6回)。
  AIは確証が持てず2つのif文に分けて安全側に倒した。**2026-07-19、検証した結果「効かない」ことを
  確認**(`invalid operation: int | none + int | none`)。実装の穴だが今回は仕様として据え置き、
  カードに「1つのifにつき1変数まで」と明記して回避策を案内した

### C. 「機能はあるが今回は使わなかった」— 穴ではなく設計判断の記録
- **判別可能union**(第5回): カードの説明は理解された上で、「フラットな数値列に分岐する形が無い
  (数値↔演算子が交互に並ぶだけで、下流で区別すべき別形状が無い)」ため不使用と判断。
  AST的な分岐(単項/二項ノード、将来の括弧対応など)が要るタスクなら使う価値があると明言。
  → 機能の存在と、その機能がタスクに適切かは別問題という健全な判断力の実証
- **判別可能union(再挑戦)**(第6回): AST分岐が要る題材で意図的に使わせたところ、自己参照の壁
  (`type alias cycle`)に当たって断念。カードが提案する回避策(名前付き再帰struct)は試されず、
  木を持たない直接評価へアーキテクチャごと変更する道を選んだ——回避策の実例が薄いカードの記述と、
  「全フィールド必須」の制約下で疑似optionalフィールドのstructを書く面倒さが、回避策を選びにくく
  している可能性がある
- **判別可能union(3度目・成功)**(第9回): 4種の相互排他な形・非再帰という「本当に活きる」題材を
  与えたところ、自発的に選んで一発成功。第5回(不使用の判断)・第6回(自己参照で失敗)と合わせて、
  「機能の存在・適切な選択・正しい実装」の3点セットが揃った検証結果になった

## 次にやること
- ~~カードの自己参照union回避策に具体例を足す~~ ✅ **2026-07-19実装**。`struct Expr { kind: string
  val: int | none  left: Expr | none  right: Expr | none }` + 1ifにつき1変数のnarrowingという
  実際に動く例をカードに追加(evalExpr(9)まで動作確認済み・e2eテスト化)
- ~~副産物として見つかったバグ: 関数名に `eval`/`arguments` を使うと、コンパイルは通るのに生成JSが
  JSのstrict mode違反で実行時crashする(`RESERVED`セット漏れ)~~ ✅ **2026-07-19解消**。
  別セッションで`RESERVED`に2語を追加(PR #4でマージ済み)
- ~~自己参照する判別可能union自体の実装(knot-tying再設計)~~ ✅ **2026-07-19実装**。
  structフィールド越しの参照(木構造・AST等)はknot-tyingで解決。structを挟まない裸の
  union同士の相互再帰だけは、flatten時に相手のplaceholderが空で型情報が消える不具合を
  避けるため、意図的に`type alias cycle`のまま(checker/e2eテスト追加)
- ~~`chan<int>[]`/`map<K,V>[]`のパース対応~~ ✅ **2026-07-19実装**。`parseSingleType()`を
  atom+配列サフィックスに分割して解消(parser/e2eテスト追加)
- ~~複数行の配列リテラルが末尾カンマ無しだと壊れる~~ ✅ **2026-07-19実装**。素の配列リテラルに
  struct/mapリテラルと同じ`skipSemis()`を追加して解消(parser/e2eテスト追加)
- ~~判別可能unionを本当に使う価値がある題材で再検証~~ ✅ **2026-07-19実施(第9回)**。
  完全一発成功・エラー0件。判別可能union検証はこれで一区切り
- ~~sort/contains/indexOf/keys/valuesの実地利用で再測定~~ ✅ **2026-07-19実施(第10回)**。
  完全一発成功・エラー0件。標準ライブラリ層の穴無しを確認
- 第11回はモジュール系(未実装: import/export)以外の未検証領域が尽きてきたので、木構造・AST
  ベースの実タスク(自己参照union解禁後の再検証)や他の中期課題(defer文・エラー表示改善等)に
  軸足を移すのが良さそう
