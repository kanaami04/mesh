# 前提台帳(Assumption Log)

「そうであると信じて進めているが、まだ検証していない事柄」の一覧。**フェーズ完了ごとに全行を見直す**。前提が崩れたら: 関連ADRを見直し → 必要なら新ADRで方針転換 → この表を更新。

| ID | 前提 | 関連ADR | 検証状況 | 検証予定 | 崩れた場合の影響 |
|---|---|---|---|---|---|
| A-1 | JS出力の性能はWebアプリ用途で実用域 | 0002 | 未検証(TS/Gleamの実績から妥当と推定) | Phase 7のTODOアプリで体感確認 | wasmバックエンド前倒し検討 |
| A-2 | ソースマップで十分なデバッグ体験を作れる | 0002 | 未検証(TSの実績から妥当と推定) | Phase 2でソースマップ出力を試作 | 独自デバッグ表示の開発が必要に |
| A-3 | Rust実装をClaude主導で保守し続けられる | 0001 | 未検証 | Phase 2の最小コンパイラで確認 | 実装言語再考(可能性低) |
| A-4 | JSランタイム相乗りで自前ランタイムを最小化できる | 0002 | 未検証 | Phase 5のstd実装で確認 | ランタイム肥大→出力サイズ悪化 |
| A-5 | JSX風UI構文がMesh文法と衝突せずパース可能 | 0003 | 机上検証の一部完了(viewブロックの字句モード分離=仕様1章L-1、state/view等の文脈キーワード化=0031) | Phase 6(9章執筆)で実証 | UI構文の記法変更(例: タグをview専用ブロック内限定にして衝突回避) |
| A-6 | リアクティビティ方式の決定はPhase 6まで遅延できる | 0003 | 未検証 | Phase 6 | 早期に型システムへ影響が出たらPhase 3で前倒し決定 |
| A-7 | unionメンバーの実行時型タグをJS出力で低コストに実現できる | 0005 | 未検証(前例で「タグ無し実装が静かに誤る」事故を確認済み。タグ方式自体の検証はこれから) | Phase 2〜3のコード生成設計で最初に検証。検証項目: 仕様2章T-9の判別可能性の対リストが実行時表現と一致すること(表現変更時はADR経由でT-9を更新)、タグのJSON.stringify/structuredClone/FFI素通し時の漏れ、noneシングルトンのFFI漏れ(仕様2章T-20特例の前提。ネストした値経由も含め10章の変換規則で保証) | union+matchの実行時表現を再設計(全値をタグ付きオブジェクトで包む方式へ変更) |
| A-8 | 名前的型付けがAI生成の成功率を大きく損なわない(TS=構造的からの負転移が許容範囲) | 0006 | 未検証 | Phase 9の白紙AI+言語カード実験で計測。言語カードに名前的である旨を明記して緩和 | 型エラーの修正提案を強化(「同じ形の別型です」と案内)。規則自体の再考は最終手段 |
| A-9 | 改行区切りの継続規則を単純な字句規則で定義しきれる(例外は後置`?`除外と括弧深度の2つで確定) | 0010→0026→0031 | **机上検証済み・欠陥2件発見→修正済み**(後置`?`の穴=0026、括弧内改行の穴=0031。確定規則は仕様1章L-19〜L-25) | Phase 2の実装で実証 | さらに例外が要る場合は明文で追加(ASI的な暗黙化はしない) |
| A-10 | ~~ブレース形のmatch腕がAI生成の成功率を大きく損なわない~~ | 0013→0016 | **前提ごと消滅**(2026-08-13、ADR-0016でブレース形を撤回し`=>`に変更したため検証不要に) | — | — |
| A-11 | 文字列のコードポイント単位の実行コストがWebアプリ用途で実用域 | 0014 | 未検証 | Phase 5のstd実装でベンチマーク | lenのキャッシュ等ランタイム最適化。単位の変更はしない(最終手段) |
| A-12 | 制約なしジェネリクスでもstdの主要操作(sortの比較等)を組み込み側で賄える | 0020 | 未検証(前実装は同方式でsortを組み込みに留めた実績あり) | Phase 5のstd設計で確認 | 制約付きジェネリクスの導入を新ADRで検討 |
| A-13 | 非同期性の推論(呼び出しグラフ+高階関数の保守的扱い)を健全に実装しきれる | 0021 | 部分検証(全asyncとの性能差はベンチ済み: benchmarks/async-overhead.mjs) | Phase 2〜3のコード生成で実装・再計測。再計測には**コールバック多用コード**(map/filter系。保守的async化が集中する箇所)を必ず含める(監査#10) | 全async出力へ後退(現実ケース1.7倍は許容範囲と実測済み。CPU重い用途のみ注意) |
| A-14 | extern宣言+検証つき変換の手間がnpm利用の摩擦として許容範囲 | 0022 | 未検証 | Phase 5で実ライブラリ(日付・HTTP系)を1つラップして体感確認。シナリオに「汎用fetchヘルパを書こうとしてjs.decodeの具体型制限(監査#8)に当たる」ケースを含める | 頻出ライブラリの標準ラッパー提供で緩和 |
| A-15 | 値意味論のコピーコストが「観測可能時のみコピー」最適化で実用域に収まる | 0033 | 未検証(前実装が同方針で「不変引数のコピーは観測不能=除去可」の分析済み) | Phase 2〜3のコード生成でベンチマーク(benchmarks/に追加) | ホットパス限定の明示コピー回避手段を検討(意味論は変えない) |
| A-16 | 参照捕捉のJS実装(クロージャ変数の共有)が値意味論の他の部分と干渉しない | 0036 | 机上検証のみ(捕捉mut変数への代入は「その変数」の更新でX-1と両立) | Phase 2〜3のコード生成で確認 | 捕捉の表現を専用セル(ボックス化)に変更 |
| A-17 | 「Mesh」は一般語で検索性が低いが、承知の上で採用した | 0039 | 検証不要(既知のトレードオフ) | — | 名前変更の理由にはしない(愛着を優先した決定) |
| A-18 | Newlineトークンのtext差(`"\n"`/`"\r\n"`)を後段(パーサ・fmt・コード生成)が区別せずに扱える | 0041 | 未検証(字句側はtext==source[span]維持を確認済み) | Phase 2のパーサ・コード生成実装で確認 | 後段の入口でNewlineのtextを見ない規約を明文化(kindのみで判定) |
| A-19 | 入れ子セグメント+絶対オフセットspanで、補間内の式パース(再帰下降)とエラー報告が素直に実装できる | 0042 | 部分検証(字句側は実装済み。パーサ側が未検証) | Phase 2のパーサ実装で確認 | フラット分割方式(ADR-0042のA案)への転換を新ADRで検討 |
| A-20 | 実用コードの文字列・補間ネストは64段(仕様L-31の上限)で足りる。64段はスタック保護として十分な余裕がある | 0042(関連: 仕様L-31) | 部分検証(深さ約500段でSIGABRTの実測に基づく設定。releaseは32KiBスタックでも安全、debugは64段に約512KiB必要=既定2MiB/8MiBに対し4〜16倍の余裕を実測) | Phase 9のドッグフーディングで実用コードの実ネスト深さを確認 | 仕様改訂でL-31の上限値を変更(textのメモリ増幅が上限に線形比例する点に留意=roadmap設計メモ) |
| A-21 | Meshの数値リテラルは無加工でJSに出力できる(桁区切り・基数接頭辞・指数はJSと共通構文) | 0043 | 部分検証(字句側でJS非互換形=先頭ゼロを全形拒否。`1_000_000`・`0xFF`・`0b1010`・`0o755`・`1.5e3` がJSで同一構文・同一値なのは実測確認済み) | Phase 2のコード生成で数値リテラルを素通し出力し、E2E(Node実行)で確認 | JSと構文が乖離する数値機能の追加時に新ADRで再検討(または生成層に正規化を追加) |
