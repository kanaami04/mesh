// 並行処理と channel:
// spawn で関数を並行に走らせ、channel で結果を受け取る

fn worker(id: int, ch: chan<string>) {
	sleep(10 * id)
	ch <- "worker ${id} done"
}

fn main() {
	ch := chan<string>(none)

	for i := 1; i <= 3; i++ {
		spawn worker(i, ch)
	}

	for i := 0; i < 3; i++ {
		msg := <-ch
		print(msg)
	}
}
